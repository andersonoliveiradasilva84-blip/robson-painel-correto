import express from "express";

const app = express();

app.get("/", (_req, res) => {
  res.send("API Painel Gestão NeoHype rodando 🚀");
});

const PORT = process.env.PORT || 3000;

app.listen(PORT, () => {
  console.log(`Servidor rodando na porta ${PORT}`);
});
